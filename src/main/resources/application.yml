springdoc:
  swagger-ui:
    path: /swagger-ui.html
spring:
  flyway:
    enabled: true
    locations: classpath:migration
    baselineOnMigrate: "true"
    url: jdbc:postgresql://localhost:5432/spaceship_rent
    user: postgres
    password: root

  datasource:
    url: jdbc:postgresql://localhost:5432/spaceship_rent
    username: postgres
    password: root


  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

SECRET: ${SECRET}