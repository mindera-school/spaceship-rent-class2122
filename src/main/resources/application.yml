spring:
  application:
    name : SpaceShipEntity Application
  flyway:
    enable: true
    locations: classpath:migration
    baselineOnmigrate: "true"
    url: jdbc:postgresql://localhost:5432/spaceship_database
    user: ruiteixeira
    password: dro

  datasource:
    url: jdbc:postgresql://localhost:5432/spaceship_database
    username: ruiteixeira
    password: dro

  jpa:
    hibernate:
      ddl:auto: validate
    show-sql: true
    properties:
      hibernate:
        formate_sql: true
  springdoc:
    swagger-ui:
      path: /swagger-ui-html

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest