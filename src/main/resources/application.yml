spring:
  application:
    name : SpaceShipEntity Application
  flyway:
    enable: true
    locations: classpath:migration
    baselineOnmigrate: "true"
    url: jdbc:postgresql://localhost:5432/spaceship_database
    user: ruiteixeira
    password: dro

  datasource:
    url: jdbc:postgresql://localhost:5432/spaceship_database
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl:auto: validate
    show-sql: false
    properties:
      hibernate:
        formate_sql: false
  springdoc:
    swagger-ui:
      path: /swagger-ui-html

  rabbitmq:
    host: localhost
    port: 5672
    username: ${RABBIT_USER}
    password: ${RABBIT_PASSWORD}

