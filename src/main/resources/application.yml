springdoc:
  swagger-ui:
    path: /swagger-ui.html
spring:
  application:
    name = Spaceship Rent
  datasource:
    url: jdbc:postgresql://localhost:5432/spaceship_rent_db
    username: joaomoutinho
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
